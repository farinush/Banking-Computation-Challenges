import React from "react";

function SectionTitle({ children }: { children: React.ReactNode }) {
  return <h2 className="text-md font-semibold font-yekan mb-3">{children}</h2>;
}
export default SectionTitle;
